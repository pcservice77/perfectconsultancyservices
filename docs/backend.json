
{
  "entities": {
    "Service": {
      "title": "Service",
      "description": "A consultancy service. Publicly readable, admin manageable.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "icon": { "type": "string" },
        "imageUrl": { "type": "string", "description": "Base64 encoded image or URL for custom icon" }
      },
      "required": ["title", "description"]
    },
    "StaffMember": {
      "title": "Staff Member",
      "description": "Team leadership and staff. Publicly readable, admin manageable.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["Founder", "Co-Founder", "Staff"] },
        "email": { "type": "string", "format": "email" },
        "contact": { "type": "string" },
        "profession": { "type": "string" },
        "imageUrl": { "type": "string", "description": "Base64 encoded photo of the staff member" }
      },
      "required": ["name", "role", "email"]
    },
    "Enquiry": {
      "title": "Enquiry",
      "description": "Customer contact submissions. Publicly creatable, admin readable.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "message": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "message"]
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "User account data and roles.",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "isAdmin": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Job": {
      "title": "Job Posting",
      "description": "Careers at PCS. Publicly readable, admin manageable.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "location": { "type": "string" },
        "type": { "type": "string" },
        "salary": { "type": "string" },
        "requirements": { "type": "string" },
        "qualifications": { "type": "string" }
      },
      "required": ["title", "location", "type", "salary"]
    },
    "JobApplication": {
      "title": "Job Application",
      "description": "Job application submissions. Publicly creatable, admin readable.",
      "type": "object",
      "properties": {
        "jobId": { "type": "string" },
        "jobTitle": { "type": "string" },
        "name": { "type": "string" },
        "mobile": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "qualification": { "type": "string" },
        "experience": { "type": "string" },
        "address": { "type": "string" },
        "otherInfo": { "type": "string" },
        "appliedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["jobId", "name", "email", "mobile"]
    },
    "TaxUpdate": {
      "title": "Tax Update",
      "description": "Industry news and summaries. Publicly readable, admin manageable.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "summary": { "type": "string" },
        "content": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "tags": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["title", "summary", "content"]
    },
    "Subscription": {
      "title": "Newsletter Subscription",
      "description": "Newsletter signups. Publicly creatable, admin readable.",
      "type": "object",
      "properties": {
        "email": { "type": "string", "format": "email" },
        "subscribedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/services/{serviceId}": {
      "schema": "#/backend/entities/Service",
      "description": "Public READ access, Admin WRITE access."
    },
    "/staff/{staffId}": {
      "schema": "#/backend/entities/StaffMember",
      "description": "Public READ access, Admin WRITE access."
    },
    "/enquiries/{enquiryId}": {
      "schema": "#/backend/entities/Enquiry",
      "description": "Public CREATE access, Admin READ access."
    },
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "User CREATE and READ/WRITE access for own document, Admin full access."
    },
    "/jobs/{jobId}": {
      "schema": "#/backend/entities/Job",
      "description": "Public READ access, Admin WRITE access."
    },
    "/applications/{applicationId}": {
      "schema": "#/backend/entities/JobApplication",
      "description": "Public CREATE access, Admin READ access."
    },
    "/taxUpdates/{taxUpdateId}": {
      "schema": "#/backend/entities/TaxUpdate",
      "description": "Public READ access, Admin WRITE access."
    },
    "/subscriptions/{subscriptionId}": {
      "schema": "#/backend/entities/Subscription",
      "description": "Public CREATE access, Admin READ access."
    }
  }
}
