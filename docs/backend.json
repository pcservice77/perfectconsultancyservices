
{
  "entities": {
    "Service": {
      "title": "Service",
      "description": "A service offered by the company.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "icon": { "type": "string" },
        "imageUrl": { "type": "string" }
      },
      "required": ["title", "description", "icon"]
    },
    "StaffMember": {
      "title": "Staff Member",
      "description": "Founder, Co-Founder, or regular staff details.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["Founder", "Co-Founder", "Staff"] },
        "email": { "type": "string", "format": "email" },
        "contact": { "type": "string" },
        "profession": { "type": "string" },
        "imageUrl": { "type": "string" }
      },
      "required": ["name", "role", "email"]
    },
    "Enquiry": {
      "title": "Enquiry",
      "description": "A contact form submission from the website.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "message": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "message"]
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "Stored user details including admin status.",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "isAdmin": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Job": {
      "title": "Job Posting",
      "description": "A career opportunity at the consultancy.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "location": { "type": "string" },
        "type": { "type": "string" },
        "salary": { "type": "string" }
      },
      "required": ["title", "location", "type", "salary"]
    },
    "TaxUpdate": {
      "title": "Tax Update",
      "description": "Latest news regarding tax laws and compliance.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "summary": { "type": "string" },
        "content": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "tags": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["title", "summary", "content"]
    },
    "Subscription": {
      "title": "Newsletter Subscription",
      "description": "User email for the newsletter.",
      "type": "object",
      "properties": {
        "email": { "type": "string", "format": "email" },
        "subscribedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/services/{serviceId}": {
      "schema": "#/backend/entities/Service",
      "description": "List of consultancy services."
    },
    "/staff/{staffId}": {
      "schema": "#/backend/entities/StaffMember",
      "description": "Team members and founders."
    },
    "/enquiries/{enquiryId}": {
      "schema": "#/backend/entities/Enquiry",
      "description": "Customer enquiries."
    },
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "User profiles and permissions."
    },
    "/jobs/{jobId}": {
      "schema": "#/backend/entities/Job",
      "description": "Job openings."
    },
    "/taxUpdates/{taxUpdateId}": {
      "schema": "#/backend/entities/TaxUpdate",
      "description": "Tax news articles."
    },
    "/subscriptions/{subscriptionId}": {
      "schema": "#/backend/entities/Subscription",
      "description": "Newsletter subscribers."
    }
  }
}
